```python

def is_prime(n):

def sum_prime_numbers():
    
    prime_numbers = []
    total  = 0
    counter = 0
    
    while len(prime_numbers) < 5:

        counter += 1 
        
        if counter == 1 or is_prime(counter) 
        
        prime_numbers.append(counter)
        
    for n in prime_numbers:
        total += n
    
    print("Total: ", total)
    
sum_prime_numbers()

```